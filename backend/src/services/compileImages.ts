import JSZip = require("jszip");
import {IMakeGameOutput} from "./makeGameFromLinter";

async function compileImages(input: IMakeGameOutput): Promise<JSZip.OutputType> {
	return null;
}

export default compileImages;